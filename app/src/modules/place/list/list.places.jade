table.table
  tr
    th Nazwa
    th Telefon
    th E-mail
    th Ukryty
    th
    th
    th
    th
  tbody
    tr(ng-repeat="place in places")
      td {{place.name}}
      td {{place.phone}}
      td {{place.email}}
      td {{place.hidden}}
      td
        //a(ui-sref="place.show({ id: place.id })") Pokaż
      td
        //a(ui-sref="place.edit({ id: place.id })") Edytuj
      td
        span.edit-btn(ng-click="removePlace(place)") Ukryj
      td
        span.edit-btn(ng-click="unhidePlace(place)") Odkryj
